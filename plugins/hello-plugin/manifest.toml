id = "com.example.hello"
name = "Hello Plugin"
version = "0.1.1"
abi_version = 1
description = "Exercises the plugin surface: UI, haptics, notify, state store, events, hot-reload state."
permissions = ["haptic", "notify", "url.open", "clipboard.set"]
